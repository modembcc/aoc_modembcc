#include <bits/stdc++.h>
using namespace std;

int main(){
    ifstream fp("input.txt");
    string line;
    if(fp.is_open()) {
        while(getline(fp, line)) {
            
        }
        fp.close();
    }
    return 0;
}